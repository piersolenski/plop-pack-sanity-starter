import { {{iconName}} as Icon } from 'react-icons/{{iconPack}}';

export default {
  name: '{{camelCase name}}',
  title: '{{sentenceCase name}}',
  type: 'document',
  icon: Icon,
  fields: [
    {
      name: 'title',
      type: 'string',
      title: 'Title',
    },
  ],
};
